
{
  "entities": {
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user account in the system, either a customer or an administrator. Does not store authentication credentials.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the User entity."
        },
        "email": {
          "type": "string",
          "description": "The user's email address, used for communication and as a unique login identifier.",
          "format": "email"
        },
        "firstName": {
          "type": "string",
          "description": "The first name of the user."
        },
        "lastName": {
          "type": "string",
          "description": "The last name of the user."
        },
        "shippingAddress": {
          "type": "string",
          "description": "The primary shipping address for the user."
        },
        "billingAddress": {
          "type": "string",
          "description": "The primary billing address for the user."
        },
        "phoneNumber": {
          "type": "string",
          "description": "The user's phone number."
        },
        "role": {
          "type": "string",
          "description": "The role of the user within the system (e.g., 'customer', 'admin')."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the user account was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp when the user account was last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "email",
        "firstName",
        "lastName",
        "role",
        "createdAt",
        "updatedAt"
      ]
    },
    "Category": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Category",
      "type": "object",
      "description": "Represents a classification for products, allowing for organized browsing.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Category entity."
        },
        "name": {
          "type": "string",
          "description": "The name of the category (e.g., 'Electronics', 'Home Goods')."
        },
        "description": {
          "type": "string",
          "description": "A detailed description of the category."
        },
        "parentCategoryId": {
          "type": "string",
          "description": "Reference to a parent category, enabling hierarchical categories. (Relationship: Category 1:N Category)"
        }
      },
      "required": [
        "id",
        "name",
        "description"
      ]
    },
    "Product": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Product",
      "type": "object",
      "description": "Represents a single product available for purchase in the catalog.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Product entity."
        },
        "name": {
          "type": "string",
          "description": "The name of the product."
        },
        "slug": {
          "type": "string",
          "description": "URL-friendly version of the product name, using Latin characters only."
        },
        "description": {
          "type": "string",
          "description": "A detailed description of the product."
        },
        "price": {
          "type": "number",
          "description": "The current selling price of the product."
        },
        "discountPercentage": {
          "type": "number",
          "description": "The discount percentage applied to the product (0-100)."
        },
        "stockQuantity": {
          "type": "number",
          "description": "The number of units of this product currently in stock."
        },
        "imageUrls": {
          "type": "array",
          "description": "An array of URLs pointing to images of the product.",
          "items": {
            "type": "string"
          }
        },
        "categoryId": {
          "type": "string",
          "description": "Reference to the category this product belongs to."
        },
        "subCategoryId": {
          "type": "string",
          "description": "Reference to the subcategory this product belongs to."
        },
        "sku": {
          "type": "string",
          "description": "Stock Keeping Unit (SKU), a unique identifier for product variations, if applicable."
        },
        "weight": {
          "type": "number",
          "description": "The weight of the product, in a standardized unit (e.g., grams, kilograms)."
        },
        "dimensions": {
          "type": "string",
          "description": "The dimensions of the product (e.g., 'LxWxH' in cm)."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the product was added to the catalog.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp when the product information was last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "name",
        "slug",
        "description",
        "price",
        "stockQuantity",
        "imageUrls",
        "categoryId",
        "createdAt",
        "updatedAt"
      ]
    },
    "Cart": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Cart",
      "type": "object",
      "description": "Represents a user's shopping cart, holding products selected for purchase.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Cart entity."
        },
        "userId": {
          "type": "string",
          "description": "Reference to the user who owns this cart. Can be null for anonymous carts. (Relationship: User 1:1 Cart)"
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the cart was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp when the cart was last updated (e.g., items added/removed)."
        }
      },
      "required": [
        "id",
        "createdAt",
        "updatedAt"
      ]
    },
    "CartItem": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "CartItem",
      "type": "object",
      "description": "Represents a single product and its quantity within a shopping cart.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the CartItem entity."
        },
        "cartId": {
          "type": "string",
          "description": "Reference to the shopping cart this item belongs to. (Relationship: Cart 1:N CartItem)"
        },
        "productId": {
          "type": "string",
          "description": "Reference to the product added to the cart. (Relationship: Product 1:N CartItem)"
        },
        "quantity": {
          "type": "number",
          "description": "The number of units of the product in the cart item."
        },
        "priceAtTimeOfAddition": {
          "type": "number",
          "description": "The price of the product when it was added to the cart, to handle price changes."
        }
      },
      "required": [
        "id",
        "cartId",
        "productId",
        "quantity",
        "priceAtTimeOfAddition"
      ]
    },
    "Order": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Order",
      "type": "object",
      "description": "Represents a completed or in-progress purchase made by a user.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Order entity."
        },
        "userId": {
          "type": "string",
          "description": "Reference to the user who placed the order. (Relationship: User 1:N Order)"
        },
        "orderDate": {
          "type": "string",
          "description": "The date and time when the order was placed.",
          "format": "date-time"
        },
        "totalAmount": {
          "type": "number",
          "description": "The total monetary amount of the order, including shipping and taxes."
        },
        "status": {
          "type": "string",
          "description": "The current status of the order (e.g., 'pending', 'processing', 'shipped', 'delivered', 'cancelled')."
        },
        "shippingAddress": {
          "type": "string",
          "description": "The shipping address used for this specific order, captured at the time of order."
        },
        "billingAddress": {
          "type": "string",
          "description": "The billing address used for this specific order, captured at the time of order."
        },
        "shippingMethod": {
          "type": "string",
          "description": "The shipping method chosen for the order (e.g., 'Standard', 'Express')."
        },
        "paymentMethod": {
          "type": "string",
          "description": "The payment method used for the order (e.g., 'Credit Card', 'PayPal')."
        },
        "trackingNumber": {
          "type": "string",
          "description": "The tracking number for the shipment, if available."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the order record was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp when the order record was last updated (e.g., status change)."
        }
      },
      "required": [
        "id",
        "userId",
        "orderDate",
        "totalAmount",
        "status",
        "shippingAddress",
        "billingAddress",
        "shippingMethod",
        "paymentMethod",
        "createdAt",
        "updatedAt"
      ]
    },
    "OrderItem": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "OrderItem",
      "type": "object",
      "description": "Represents a single product and its quantity included in a specific order.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the OrderItem entity."
        },
        "orderId": {
          "type": "string",
          "description": "Reference to the order this item belongs to. (Relationship: Order 1:N OrderItem)"
        },
        "productId": {
          "type": "string",
          "description": "Reference to the product purchased. (Relationship: Product 1:N OrderItem)"
        },
        "quantity": {
          "type": "number",
          "description": "The number of units of the product purchased in this order item."
        },
        "pricePerUnit": {
          "type": "number",
          "description": "The price of a single unit of the product at the time of purchase."
        },
        "subtotal": {
          "type": "number",
          "description": "The subtotal for this order item (quantity * pricePerUnit)."
        }
      },
      "required": [
        "id",
        "orderId",
        "productId",
        "quantity",
        "pricePerUnit",
        "subtotal"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "google.com"
    ]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "User",
      "description": "Stores user profiles."
    },
    "/categories/{categoryId}": {
      "schema": "Category",
      "description": "Product categories."
    },
    "/products/{productId}": {
      "schema": "Product",
      "description": "Product catalog."
    },
    "/users/{userId}/carts/{cartId}": {
      "schema": "Cart",
      "description": "User shopping carts."
    },
    "/users/{userId}/carts/{cartId}/items/{itemId}": {
      "schema": "CartItem",
      "description": "Items in a user's cart."
    },
    "/orders/{orderId}": {
      "schema": "Order",
      "description": "Customer orders."
    },
    "/orders/{orderId}/items/{itemId}": {
      "schema": "OrderItem",
      "description": "Items in a customer's order."
    },
    "admin_roles/{uid}": {
      "schema": "User",
      "description": "Admin users list."
    }
  }
}
